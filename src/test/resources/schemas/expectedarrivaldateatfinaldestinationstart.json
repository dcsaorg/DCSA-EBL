{
  "type": "string",
  "description": "The start date (provided as a range together with `expectedArrivalDateEnd`) for when the shipment is expected to arrive at final destination. If vessel/voyage or expected date of departure is not provided, this is mandatory together with `expectedArrivalDateEnd`\n",
  "format": "date",
  "example": "2021-05-17",
  "$schema": "http://json-schema.org/schema#"
}